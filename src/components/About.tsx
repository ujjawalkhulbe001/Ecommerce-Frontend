const About: React.FC = () => {
  return (
    <div>
      {/* Render an h1 element with the text 'About Us'. */}
      <h1>About Us</h1>

      {/* Render a paragraph with some description about the company. */}
      <p>We are a company that values...</p>
    </div>
  );
};

// Export the 'About' component as the default export.
// This allows the component to be imported with the name 'About' from this file.
export default About;
